chromadb==1.4.0
sentence-transformers==5.2.0
python-dotenv==1.2.1
flask>=3.0.3,<4
flask-cors==4.0.1
# pgvector backend for the local KB store. psycopg_pool is shipped via
# the [pool] extra and gives us a small connection pool with built-in
# health-checking and reconnect.
psycopg[binary,pool]>=3.2.10,<3.4
pgvector>=0.3.6,<0.5

# ---- KB ingest pipeline (scripts/kb_parsers/) ----
# Multi-format source parsers for `kb-ingest.py`. Each format has its
# own parser module; pulling these in here keeps `pip install -r
# requirements.txt` enough to bootstrap a fresh dev env. Image / scanned
# PDF OCR additionally requires the `tesseract` binary + chi_sim
# language pack on the host (brew install tesseract tesseract-lang).
pdfminer.six>=20240706
python-docx>=1.1.2
python-pptx>=1.0          # .pptx slides (PR #32)
pdf2image>=1.17.0
pytesseract>=0.3.10
Pillow>=10.4
beautifulsoup4>=4.12     # for .htm/.html siblings in the corpus
PyYAML>=6.0              # used by markdown frontmatter parser
