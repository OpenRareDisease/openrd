# v2.5.0 部署手册

> 覆盖 `v2.4.0` → `v2.5.0` 升级路径：env 准备、迁移 013–018、prod 部署顺序、监控、回滚。
>
> **本手册取代 `docs/runbooks/v2.4.0-deploy.md`。** 那份手册对当前仓库已有多处失真：迁移范围写到 012、§4.1 的回滚命令会留下 `schema_migrations` 幽灵行、§1.1 的必填表没有 `NODE_ENV`、§1.3 指向一个不存在的 `apps/api/requirements.txt`、§3.2 step 7 让人去推 TestFlight / Play Store 而仓库根本产不出原生包。v2.4.0 手册保留为历史归档，**不要**照它部署 v2.5.0。
>
> 与通用 `docs/release-checklist.md` 配合使用：那份是每次发布都要过的门禁，本手册补 v2.5.0 specific 的步骤。

## 0. 这次发了什么

`v2.5.0` 没有新的患者侧功能面，主线是一轮 88 条 finding 的 deploy-readiness 审计闭合——即「测试全绿」和「生产能跑」之间那一层。按影响部署动作的顺序：

- **生产 fail-fast 真的覆盖到了它声称的东西**：新增 AI key 缺失、`postgres:postgres` 凭据（旧检查只认 `localhost` 字面串，在 compose 路径上是永久空转）、密钥强度、Baidu OCR 凭据、非 `.cn` 的 `AI_API_BASE_URL`（PIPL 跨境）、生产用本地磁盘存储、明文 MinIO 六道闸门。**这意味着一个在 v2.4.0 能起来的 `.env`，在 v2.5.0 可能起不来**——这是有意的，见 §1.1。
- **compose 变成了它文档里描述的那套部署**：`DATABASE_URL` 改成插值（此前 operator 填的托管库连接串被静默丢弃）、`POSTGRES_PASSWORD` 强制必填、`minio` 加入 `prod` profile、每个服务有 `mem_limit` 和日志上限、api 有 `stop_grace_period`、kb-service 不再拿到整份 `.env`。
- **readiness 不再把「KB 热了没」当成「API 能不能服务」**：`/api/healthz/ready` 只看 database + embedded OCR；KB / 对象存储 / 未配置的 AI key 走 `degraded`。compose 依赖放宽成 `service_started`。
- **`/api/healthz` 不再对公网吐内部细节**：生产下非 loopback 调用方只拿到 status，详情进日志并回一个 `requestId`。
- **迁移能真回滚了**：`migrate --down <id>` 把 `_down.sql` 和 ledger 删除放进同一个事务。
- **备份变成了代码**：`scripts/db-backup.sh` / `scripts/db-restore.sh`。
- 数据库迁移 **013–018**（v2.4.0 手册停在 012）。

完整清单见 [CHANGELOG.md](../../CHANGELOG.md) 的 `v2.5.0` 条目。

---

## 1. 部署前必备项

### 1.0 前置：`.env` 必须存在，且必须有 `POSTGRES_PASSWORD`

`docker-compose.yml` 用 `env_file: [{path: .env, required: false}]`，所以 `.env` 缺失本身不再是硬失败。但 compose 里有两个**没有默认值**的插值：

```
POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env (local dev can use POSTGRES_PASSWORD=postgres)}
```

缺它 `docker compose config` 直接拒绝渲染，一个容器都不会创建。所以第 0 步永远是：

```bash
cp .env.example .env
# 然后按 §1.1 逐行填，至少补上 .env.example 里没有的 POSTGRES_PASSWORD
```

> ⚠️ `POSTGRES_PASSWORD` 目前**不在** `.env.example` 里，需要手工加一行。本地开发 `POSTGRES_PASSWORD=postgres` 就够；生产必须换成真密码——`validateProductionEnv` 会独立拒绝任何仍带 `postgres:postgres` 的 `DATABASE_URL`，无论主机名是 `localhost` 还是 compose 内网的 `postgres`。

### 1.1 必填 env 变量

`apps/api/src/config/env.ts#validateProductionEnv` + `#validateStorageEnv` 校验下列项；缺一项 prod 启动 fail-fast（错误信息会点名缺哪个）。

#### 无条件必填

| 变量                                                   | 不允许的值                                             | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------ | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`NODE_ENV`**                                         | 不设 / `development`                                   | **必须是 `production`（或 `staging`）。这一行不填，下面整张表都不生效**——`validateProductionEnv` 开头就是 `if (!env.isProductionLike) return errors;`，非生产模式下它返回空错误列表、一条日志都不打。`change-me-super-secret`、`CORS_ORIGIN=*`、`OTP_PROVIDER=mock`、仓库里公开的 KB 占位 token、明文 PHI 数据库连接——全部照过。v2.4.0 手册漏了这一行，是本次审计里唯一一条「让所有其它护栏一起失效」的配置。compose 用 `${NODE_ENV:-development}` 从同一份 `.env` 插值，所以写在 `.env` 里就生效。 |
| `JWT_SECRET`                                           | `change-me-super-secret` / 任何 `dev-only-*` 占位      | ≥32 字符且 ≥10 个不同字符。用 `openssl rand -hex 32`。（不是字符类规则——hex 输出只有小写+数字两类，但有 128 bit 熵；这条规则挡的是「把一句口令重复填到 32 位」。）                                                                                                                                                                                                                                                                                                                                  |
| `OTP_HASH_SECRET`                                      | 同上                                                   | 同上。OTP 是 6 位数字，这把 HMAC key 泄了，存的 hash 就退化成 10^6 查表。                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `DATABASE_URL`                                         | 任何 userinfo 是 `postgres:postgres` 的连接串          | 主机名不限——compose 内网的 `postgres:5432` 是合法生产拓扑，被拒的是仓库里公开的那对凭据（它还是 Postgres 超级用户）。                                                                                                                                                                                                                                                                                                                                                                               |
| `DATABASE_SSL_ENABLED` 或 `DATABASE_ALLOW_INSECURE`    | 两个都不设                                             | 二选一必须显式表态，见 §1.4。                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `CORS_ORIGIN`                                          | `*` 或空                                               | 显式列域名（多个用 `,` 分隔），如 `https://ai.fshdyouth.com`。                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OTP_PROVIDER`                                         | `mock`                                                 | `tencent` 或 `internal_test`，各自的附加必填见下表。                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OCR_PROVIDER`                                         | `mock`                                                 | `embedded` 或 `baidu`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `AI_API_KEY` 或 `OPENAI_API_KEY`                       | 两个都空                                               | **本次新增。** 此前缺 key 的 prod 会干净启动、`ready: true`、健康容器，然后每个患者提问都返回「AI 服务未配置」——而第一小时监控看到的是「AI 调用量为 0」，读起来像用量低而不是配错了。                                                                                                                                                                                                                                                                                                               |
| `KB_SERVICE_TOKEN`                                     | 空 / `dev-only-local-token-NOT-FOR-PROD`               | ≥32 字符随机串；API 和 KB 服务两边必须同一个值。                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `AI_CROSS_BORDER_ACKNOWLEDGED`                         | `false` **且** `AI_API_BASE_URL` 主机名不以 `.cn` 结尾 | **本次新增。** 每次 prompt 都带患者原话 + 同意闸门放行的临床字段，把 endpoint 换到 `.com` 就是健康数据出境（PIPL 第 38–39 条：CAC 安全评估或已备案标准合同、单独同意、载明境外接收方）。合规到位后设 `true`，否则用 `.cn` endpoint。                                                                                                                                                                                                                                                                |
| `STORAGE_PROVIDER=minio` 或 `STORAGE_ALLOW_LOCAL=true` | 生产用 `local` 且没显式确认                            | **本次新增。** 生产建议 `minio`（见 §4.3）；确实要用容器卷就显式设 `STORAGE_ALLOW_LOCAL=true`，表示你自己有备份方案。                                                                                                                                                                                                                                                                                                                                                                               |

#### 按分支必填

| 触发条件                     | 追加必填                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `OTP_PROVIDER=tencent`       | `TENCENT_SECRET_ID` / `TENCENT_SECRET_KEY` / `TENCENT_SMS_SDK_APP_ID` / `TENCENT_SMS_SIGN_NAME` / `TENCENT_SMS_TEMPLATE_ID`（地域默认 `ap-guangzhou`）                                                                                                                                                                                           |
| `OTP_PROVIDER=internal_test` | 非空 `OTP_TEST_PHONE_ALLOWLIST` + 合法 `OTP_TEST_FIXED_CODE`（位数等于 `OTP_CODE_LENGTH`，默认 6）。见 §1.6                                                                                                                                                                                                                                      |
| `OCR_PROVIDER=baidu`         | `BAIDU_OCR_API_KEY` + `BAIDU_OCR_SECRET_KEY`。**本次新增这道检查**：此前缺任一个，provider 工厂会静默回落到 embedded 解析器，没有任何信号                                                                                                                                                                                                        |
| `STORAGE_PROVIDER=minio`     | `MINIO_ENDPOINT` / `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY`（`minioadmin` / `minioadmin12345678` 在生产被拒），且 `MINIO_USE_HTTPS=true` **或** `MINIO_ALLOW_INSECURE=true`。后者只在 MinIO 就在 compose 内网、走 docker bridge 时才可接受；远程 / 托管 S3 兼容 endpoint 必须开 TLS，否则 access key、secret key 和每一份扫描件的原始字节明文过网 |

#### 调参项（有默认值，但你需要知道它们存在）

这四个变量各自有一个耦合关系，代码注释里写了但没人会去读：

| 变量                          | 默认  | 耦合                                                                                                                                      |
| ----------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `DATABASE_POOL_MAX`           | 10    | `实例数 × POOL_MAX` 必须小于 Postgres 的 `max_connections`（默认 100）                                                                    |
| `DATABASE_CONNECT_TIMEOUT_MS` | 10000 | —                                                                                                                                         |
| `SHUTDOWN_READINESS_DRAIN_MS` | 5000  | 要大于负载均衡器的健康检查间隔。本仓库的 Caddy **不做**健康轮询（`Caddyfile` 里没有 `health_uri`），单副本部署下这 5 秒是纯等待，可以设 0 |
| `SHUTDOWN_GRACE_MS`           | 20000 | 必须小于 compose 的 `stop_grace_period`（已设 25s）。这个 deadline 从 t=0 起算、**包含**上面那 5 秒 drain，不是叠加                       |

其余可选项（`AI_MAX_TOOL_ROUNDS`、`AI_HISTORY_MAX_MESSAGES`、`AI_HISTORY_CHAR_BUDGET`、`KB_FETCH_K` / `KB_FINAL_N` / `KB_MAX_PER_SOURCE`、`KB_MAX_CONCURRENT_SEARCHES`、`KB_SEARCH_QUEUE_WAIT_SECONDS`、`OCR_MAX_PDF_PAGES`）都在 `.env.example` 里带注释，compose 也把它们显式列进了 `environment:` 块，不用翻代码。

### 1.2 KB 服务必要变更

Python KB 服务的 `__main__` 在下列任一情形下拒绝启动：

- `KB_SERVICE_HOST=0.0.0.0` 且 `KB_SERVICE_TOKEN` 为空
- `NODE_ENV` ∈ {`production`, `staging`} 且 token 为空，**或** token 是仓库里公开的 `dev-only-local-token-NOT-FOR-PROD`

> 本次修正：`staging` 以前不在这道守卫里（只认 `production`），而 `env.ts` 的 `isProductionLike` 两个都算。runbook §3.2 是先起 kb-service 再起 api，那个窗口里 staging 的 KB 会用一个公开 token 对外服务整个语料库。现在两边定义一致了。

compose 已经把 `NODE_ENV` 显式传给 kb-service（`${NODE_ENV:-development}`），不再只靠 `env_file` —— 因为 `env_file: .env` 已经从 kb-service 上撤掉了。

> **为什么撤掉**：那一行把 `JWT_SECRET`、`OTP_HASH_SECRET`、`AI_API_KEY` 和腾讯短信凭据一起塞进了一个只做向量检索的容器。撤之前已经把 kb-service 真正读的每一个变量都显式列进 `environment:`（`NODE_ENV`、`KB_*`、`HF_*`、四个 `CHROMA_*` 回退路径、`DATABASE_URL`、`KB_SERVICE_TOKEN`）。如果你给 kb-service 加了新的 `os.getenv()`，**记得同时加到 compose 的 `environment:` 块**，否则它在容器里读不到。

### 1.3 依赖

三个 requirements 文件，各有各的消费者，不要互相串：

| 文件                                        | 谁装它                                 | 内容                                                                                                                                                                                                                                                  |
| ------------------------------------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/requirements-embedded-report.txt` | `apps/api/Dockerfile`（api 镜像）      | PyPDF2 / Pillow / pytesseract / pdf2image / requests / json-repair / python-dotenv。**没有 paddleocr**，见下                                                                                                                                          |
| `apps/api/requirements-kb.txt`              | `apps/api/Dockerfile.kb`（kb 镜像）    | sentence-transformers / python-dotenv / psycopg[binary,pool] / pgvector。本次新增，此前 kb 镜像装的是仓库根 `requirements.txt`，把 chromadb（连带 onnxruntime、grpcio、opentelemetry、kubernetes client）和整套 ingest 工具链装进了存放医学语料的容器 |
| 仓库根 `requirements.txt`                   | 开发机 bootstrap + `npm run kb:ingest` | ingest 侧的解析器全家桶                                                                                                                                                                                                                               |

> v2.4.0 手册 §1.3 让你检查 `apps/api/requirements.txt` 里有没有 `paddleocr`。**那个文件不存在，paddleocr 也不在仓库任何一个 requirements 里。** 生产 OCR 是 Tesseract-only，`apps/api/Dockerfile` 的 `ENV OCR_DISABLE_PADDLE=true` 是有意的（PaddleOCR 实测峰值 6.4 GB，远超 api 容器 3g 的 `mem_limit`）。现在引擎会在启动时打到 stderr，解析结果里也带 `ocr_engine` 字段，不用再靠猜。要改回 Paddle 就得同一次改动里加依赖、去掉那行 ENV、调 `mem_limit` 和 `OCR_PARSER_TIMEOUT_MS`。

> ⚠️ **国内服务器 build**：`Dockerfile.kb` 装重型 ML 依赖默认走 `pypi.org`，过 GFW 只有 ~17 KB/s。Dockerfile 已用 `ARG PIP_INDEX_URL` 默认指向清华镜像，国内 build ~14 MB/s；海外 build 用 `--build-arg PIP_INDEX_URL=https://pypi.org/simple` 覆盖。torch 走 PyTorch 自己的 CDN，不受影响。

> ⚠️ **国内服务器 runtime**：kb-service 首次 warmup 从 HuggingFace 拉 `bge-m3`（~2GB）。**`HF_ENDPOINT` 的默认值本次已改成 `https://hf-mirror.com`**（compose 和 `knowledge_service.py` 两处），和 `Dockerfile.kb` 早就做过的 pip 镜像决定对齐。海外部署反过来要显式设 `HF_ENDPOINT=https://huggingface.co`。

### 1.4 Postgres extension 与 SSL

prod 数据库需要 `vector` 扩展：

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

托管 PG（RDS、Cloud SQL）通常要先在控制台把 `vector` 加进扩展白名单。自管 `pgvector/pgvector:pg16` 容器已预装扩展库，`db/init_db.sql` 在数据库**首次 init 时**就执行 `CREATE EXTENSION vector`。

> ⚠️ 注意 `db/init_db.sql` 里那行不是「非 compose 场景的幂等兜底」——`CREATE EXTENSION IF NOT EXISTS` 仍然要读控制文件，宿主上没装 pgvector 时它在 bootstrap 阶段就会失败，根本走不到 migration 006。这条注释本次已在 `init_db.sql` 里更正。

> ⚠️ **若 postgres 数据卷是在此修复之前建的**（init script 只在卷为空时跑一次），扩展不会自动补上，kb 会一直 unhealthy：
>
> ```bash
> docker compose --profile prod exec -T postgres \
>   psql -U postgres -d fshd_openrd -c "CREATE EXTENSION IF NOT EXISTS vector;"
> docker compose --profile prod up -d
> ```

**DB SSL**：`DATABASE_SSL_ENABLED` 单独控制，**不被 `NODE_ENV` 强制**。远程 / 托管 DB 必须显式 `DATABASE_SSL_ENABLED=true`（按需配 `DATABASE_SSL_REJECT_UNAUTHORIZED`）。compose 内部 postgres 走 docker 内网、不支持 SSL，自管栈保持 `DATABASE_SSL_ENABLED=false` **并设 `DATABASE_ALLOW_INSECURE=true`** 显式确认——否则 prod 启动 fail-fast。pool / migrate client / bootstrap admin client 三处共用 `resolvePgSsl`。

> **本次修正**：`ensureDatabaseExists` 以前无条件先连 `postgres` 维护库。托管 PG 上应用角色往往连不上它，结果 api 容器在碰 ledger 之前就 crash-loop，报错还指着一个 operator 没配过的库名。现在先试目标连接串，只有拿到 SQLSTATE `3D000`（invalid_catalog_name）才回落到维护库；密码错、路由不通这些照原样抛出。

### 1.5 对外暴露与 HTTPS（Caddy 反代）

患者数据必须走 HTTPS。`caddy` 服务（`prod` profile）做反代 + 自动证书：

- **域名**：`DOMAIN`（默认 `ai.fshdyouth.com`）的 A 记录指向服务器公网 IP。Caddy 用 HTTP-01 challenge 自动申请 + 续期 Let's Encrypt 证书。
- **安全组只放行 `80` + `443`**。**不要**放行 `4000` / `8080` / `5432` / `5010` / `9000`——它们在 compose 里绑 `127.0.0.1` 或只在内网。
- **路由**：`/api/*` → `api:4000`（含 SSE），`/*` → `web:80`。
- **安全响应头**（本次新增，Caddy 站点级 + `nginx.conf` server 级各一份）：`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`、`Permissions-Policy`、`Strict-Transport-Security: max-age=31536000; includeSubDomains`，静态站另加 `Content-Security-Policy: frame-ancestors 'none'`。
  - HSTS 是一年期承诺：浏览器见过之后一年内拒绝对本域走明文 HTTP。**故意没有 `preload`**——那个只能靠申请撤销。
  - CSP 只在 web upstream 的 `handle` 里设，不设站点级：站点级会**替换掉** helmet 在 `/api` 上已经发的那份更完整的策略。
- **部署命令必须带 `--profile prod`**：

  ```bash
  docker compose --profile prod up -d --build
  ```

  这一条命令现在能把**全部六个服务**拉起来：`postgres`、`kb-service`、`api`、`web`（无 profile，默认启动）+ `minio`、`caddy`（profile 分别是 `['minio','prod']` 和 `['prod']`）。

  > **本次修正**：`minio` 以前只在 `['minio']` profile 里，而手册 §4.3 又要求 prod 必须走 `STORAGE_PROVIDER=minio`。结果是整栈起来全绿、healthcheck 通过、Caddy 放进流量，然后每一次患者上传都在 connect 阶段 500——第一个信号是患者传不上 MRI 报告。现在 `--profile prod` 会带上它。**不需要**再叠一个 `--profile minio`。

- **移动端**：见 §3.2 step 6，**不要**去改仓库根 `.env`。
- **CORS**：`.env` 设 `CORS_ORIGIN=https://ai.fshdyouth.com`。

首次拉起后 `docker compose --profile prod logs -f caddy` 看到 `certificate obtained successfully` 即签发成功。

> ⚠️ **备案**：大陆区域的服务器，未备案域名的 80/443 入站会被拦，Caddy 的 HTTP-01 challenge 会一直失败、站点起不来，而日志里只会看到取证循环。部署前确认 `fshdyouth.com` 的 ICP 备案号，并确认站点页脚已渲染它。这也是 `docs/release-checklist.md` §1 的一条门禁。

### 1.6 OTP 通道

腾讯短信 provider 已经实现并接线（`apps/api/src/services/otp/tencent-otp-provider.ts`，基于腾讯云 `SendSms` v20210111 API）。

> v2.4.0 手册 §1.6 开头写「`TencentOtpProvider` 目前是未实现的桩，发码会 501」，十行之后又写「已实现」——**前半句是错的，本次删除**。照它选了 `internal_test` 的后果是：只有白名单里的号能登录，**任何真实新患者都注册不进来**，生产 app 变成团队专用。

**生产默认走 `tencent`**：`.env` 设 `OTP_PROVIDER=tencent` + 五个凭据（`TENCENT_SECRET_ID` / `TENCENT_SECRET_KEY` 来自 CAM，`TENCENT_SMS_SDK_APP_ID` / `TENCENT_SMS_SIGN_NAME` / `TENCENT_SMS_TEMPLATE_ID` 来自短信控制台）。

⚠️ **模板必须是 2 个变量**：`{1}`=验证码、`{2}`=有效期分钟，和代码发的 `TemplateParamSet: [code, ttlMinutes]` 对齐。个数不符腾讯云直接拒发（`TemplateParamSetNotMatchApprovedTemplate`）。缺任一凭据 → prod 启动 fail-fast。发送失败统一返回 502「短信发送失败，请稍后重试」，不泄露腾讯云内部错误。

`internal_test` 是**受控测试窗口**的显式选择，不是短信没就绪时的替代品：

- `OTP_TEST_PHONE_ALLOWLIST`：逗号分隔白名单。**只有**这些号能登录，其他号发码直接 403。号码可写裸号（`13922220001`）或带国码（`+8613922220001`），后端 `normalizePhone` 两种都能匹配。
- `OTP_TEST_FIXED_CODE`：固定验证码（`OTP_CODE_LENGTH` 位数字）。
- 护栏：prod/staging 下白名单为空或固定码非法 → 启动 fail-fast。验证码**不会**进 HTTP 响应。
- ⚠️ 固定码不像随机 OTP 那样一次性失效，它一直有效直到你改 env。**测试设备丢失或固定码外泄时立即轮换并重启容器。**

### 1.7 备份先行

**迁移前永远先备份**，而且现在这是一条脚本而不是一句嘱咐：

```bash
BACKUP_DIR=/mnt/offsite/openrd npm run db:backup
```

`scripts/db-backup.sh` 做了四件手打 `pg_dump` 不会做的事：

1. **预检**：`kb_chunks` 行数低于 `MIN_KB_CHUNKS`（默认 1）或 `app_users` 读不到就拒绝出档——一份语料为空时拍的 dump 看起来完全正常，而 547 MB 的源语料只在一台笔记本上。
2. 先写 `.partial` 再改名，所以被中断的 dump 不会长得像一份完整备份。
3. 用 `pg_restore --list` 回读校验通过之后，才允许 `RETENTION_DAYS`（默认 14）去清旧档。
4. `BACKUP_DIR` 落在工作区内时自动丢一个自忽略的 `.gitignore`，PHI dump 不会被 `git add -A` 卷进去。

> `BACKUP_DIR` 必须指到**宿主机之外**才算备份。和 `pg-data` 同一块盘上的 dump，扛不住任何一种真正会发生的故障。这条脚本是纯 shell，可以直接挂 host cron（脚本头部有 crontab 示例）。

恢复演练用 `npm run db:restore -- <dump> --into postgres://…/openrd_restore_drill`。默认拒绝任何已有业务行的目标库，`--force` 才覆盖——**在生产上永远不要带 `--force`**。

---

## 2. Staging 验证 checklist

### 2.1 数据库迁移（013 → 018）

> 本次交付的迁移是 **013 到 018**，没有 019。v2.4.0 手册写的「migration 003→012 跟随 api 启动一并完成」在当前树上是错的。

#### Pre-flight — `schema_migrations` 里有 `*_down.sql` 行吗？

PR #58 之前的 `migrate.ts` 会把 `011` / `012` 的 `_down.sql` 当 forward 跑掉，DROP 掉刚建的 CHECK + trigger，并往 ledger 写两条 `*_down.sql` 行。**任何在 PR #58（commit `bbd403f`）之前部署过 master 的环境都中招。** 从 v2.4.0 升上来的环境已经处理过；只有更老的环境需要这一段。

```sql
-- 1) 看 ledger 有没有 _down 行
SELECT id FROM schema_migrations WHERE id LIKE '%_down.sql';
-- 期待：(0 rows)

-- 2) 有的话，把对应 forward 行一起删掉（schema 里 CHECK / trigger 也是空的，要重跑）
DELETE FROM schema_migrations
 WHERE id IN (
   '011_status_check_constraints.sql',
   '011_status_check_constraints_down.sql',
   '012_text_column_constraints.sql',
   '012_text_column_constraints_down.sql'
 );

-- 3) 重跑 forward
--   npm run db:migrate
-- 期待 stdout: Applied 011_… / Applied 012_…；_down 文件不该出现

-- 4) 验证 CHECK + trigger 到位
SELECT COUNT(*) FROM pg_constraint WHERE contype='c' AND conname LIKE '%_check'; -- ≥ 22
SELECT tgname FROM pg_trigger WHERE tgname='patient_followup_events_same_profile'; -- 1 行
```

#### 常规迁移

```bash
# 1. 备份（见 §1.7；这是门禁不是建议）
BACKUP_DIR=/mnt/offsite/openrd npm run db:backup

# 2. 先看一眼将要发生什么
npm run db:migrate:status
#   applied / pending / drifted 三态。`drifted` = ledger 里记的 SHA-256
#   和磁盘上的文件对不上（最常见的原因是有人改了注释）。它只报告不拦截。

# 3. 应用
npm run db:migrate
```

#### 013 → 018 逐条说明与验证

**每一条都是幂等的**（`ADD COLUMN IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS`），除了 015 和 017 的 `ADD CONSTRAINT`——Postgres 没有 `ADD CONSTRAINT IF NOT EXISTS`，重复跑会抛 42710。runner 按 ledger 跳过已应用文件，正常路径不会重复跑。

| #   | 做什么                                                                                                  | 锁 / 风险                                                                                      | 验证                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| 013 | `ai_prompt_audit` 加 `history_message_count`（`NOT NULL DEFAULT 0`）、`history_char_length`（nullable） | PG11+ 的带默认值加列不重写表，瞬间完成                                                         | `\d ai_prompt_audit` 有两列                                                           |
| 014 | 新建 `account_deletion_requests` + 两个索引（删除权 / 冷静期账本）                                      | 新表，无锁风险。**故意没有 user_id 外键**：这张表要证明删除发生过，必须活过它描述的那次 DELETE | `SELECT to_regclass('account_deletion_requests');` 非空                               |
| 015 | `patient_function_tests.unit` 归一化 + CHECK                                                            | **有数据改写，见下**                                                                           | 见下                                                                                  |
| 016 | 三张随访表加 `deleted_at`（软删除）+ 三个 partial 索引                                                  | 加 nullable 列 + 建索引；当前数据量下毫秒级                                                    | `SELECT count(*) FROM pg_indexes WHERE indexname LIKE '%_profile_live';` = 3          |
| 017 | `patient_function_tests` 加 `not_applicable`（「今天做不了」）+ CHECK + partial 索引                    | `ADD CONSTRAINT` 走 ACCESS EXCLUSIVE + 校验扫描；列有 `DEFAULT FALSE`，每行都平凡满足          | 见下                                                                                  |
| 018 | `patient_measurements` 加 `(muscle_group, strength_score)` 索引（队列对比查询）                         | 普通 `CREATE INDEX` 取 SHARE 锁：**挡写不挡读**。当前 233 行、9 页，毫秒级                     | `SELECT indexname FROM pg_indexes WHERE indexname='idx_patient_measurements_cohort';` |

**015 要单独看一眼**。它对 `patient_function_tests.unit` 做三步：把已有值抄进新列 `unit_legacy` → 按别名表（`s`/`秒`→`sec`，`米`→`m`，`次`→`reps`…）归一化 → **把剩下认不出来的值 NULL 掉**，然后加 CHECK。

- 生产从没跑过 015（`v2.4.0` tag 的迁移目录停在 012），所以这次是第一次执行，那个 NULL 化 UPDATE 会在新镜像启动的一瞬间发生。
- 本次修改在 NULL 化**之前**先把原文抄进 `unit_legacy`，`_down` 脚本因此能真的还原。此前它只 DROP 掉 CHECK，头部却写着「reverse migration 015」——一个它做不到的承诺。
- 迁移后看一眼有多少行被动过：

```sql
-- 被 NULL 掉的自由文本（原文还在 unit_legacy 里）
SELECT unit_legacy, count(*) FROM patient_function_tests
 WHERE unit IS NULL AND unit_legacy IS NOT NULL
 GROUP BY 1 ORDER BY 2 DESC;
```

**两个新 CHECK 的验证**：

```sql
SELECT conname FROM pg_constraint
 WHERE conname IN ('patient_function_tests_unit_check',
                   'patient_function_tests_not_applicable_check');
-- 期待 2 行
```

015 / 017 的 `ADD CONSTRAINT` 都**不带** `NOT VALID`，和 011/012 的惯例不同——这是有意的，两份文件头部各写了理由（`NOT VALID` 只省下添加时那一次扫描，之后每次 UPDATE 仍然重查整行，会让 016 的软删除更新在旧数据上失败）。当前表规模下 ACCESS EXCLUSIVE 只持有毫秒级，而且 compose 是单 api 实例、`up -d --build` 先停旧容器再迁移，没有并发写者。**表长到十万行以上再回来重新考虑拆成 `NOT VALID` + 单独排期的 `VALIDATE CONSTRAINT`。**

#### 历史数据 VALIDATE（011/012 的 NOT VALID 约束，可选）

这一段和 v2.4.0 手册一致，**只在你还没跑过它**的环境需要。跑全表扫描，放维护窗口。

```bash
psql $STAGING_DB <<'EOF'
ALTER TABLE app_users               VALIDATE CONSTRAINT app_users_role_check;
ALTER TABLE auth_otps               VALIDATE CONSTRAINT auth_otps_status_check;
ALTER TABLE auth_otps               VALIDATE CONSTRAINT auth_otps_purpose_check;
ALTER TABLE otp_verification_codes  VALIDATE CONSTRAINT otp_verification_codes_scene_check;
ALTER TABLE patient_submissions     VALIDATE CONSTRAINT patient_submissions_kind_check;
ALTER TABLE patient_documents       VALIDATE CONSTRAINT patient_documents_status_check;
ALTER TABLE patient_documents       VALIDATE CONSTRAINT patient_documents_type_check;
ALTER TABLE patient_measurements    VALIDATE CONSTRAINT patient_measurements_side_check;
ALTER TABLE patient_measurements    VALIDATE CONSTRAINT patient_measurements_entry_mode_check;
ALTER TABLE patient_medications     VALIDATE CONSTRAINT patient_medications_status_check;
ALTER TABLE patient_followup_events VALIDATE CONSTRAINT patient_followup_events_severity_check;
ALTER TABLE ai_prompt_audit         VALIDATE CONSTRAINT ai_prompt_audit_consent_level_check;
ALTER TABLE ai_prompt_audit         VALIDATE CONSTRAINT ai_prompt_audit_redaction_mode_check;
ALTER TABLE ai_prompt_audit         VALIDATE CONSTRAINT ai_prompt_audit_status_check;
ALTER TABLE patient_activity_logs   VALIDATE CONSTRAINT patient_activity_logs_source_check;
ALTER TABLE patient_function_tests  VALIDATE CONSTRAINT patient_function_tests_type_check;
ALTER TABLE ai_consent_events       VALIDATE CONSTRAINT ai_consent_events_note_length_check;
EOF
```

任何一条 VALIDATE 失败 = 历史数据违反约束。最可能的两列是 `patient_documents.document_type`（v2.3.1 时代直接写 OCR 子类型）和 `patient_documents.status`（老值 `pending` / `queued`）：

```sql
SELECT document_type, COUNT(*) FROM patient_documents
 WHERE document_type NOT IN ('mri','genetic_report','blood_panel','other')
 GROUP BY 1 ORDER BY 2 DESC;

SELECT status, COUNT(*) FROM patient_documents
 WHERE status NOT IN ('uploaded','processing','processed','parsed','needs_review','failed','parse_failed')
 GROUP BY 1 ORDER BY 2 DESC;
```

back-fill 语句见 `docs/runbooks/v2.4.0-deploy.md` §2.1「Pre-flight backfill」，未改动。也可以选择永久留 NOT VALID：CHECK 仍管新写入，旧数据不动。

### 2.2 KB 服务联通

```bash
# 未带 token
curl -X POST http://staging-kb:5010/multi \
  -H 'Content-Type: application/json' -d '{"question":"FSHD 是什么"}'
# 期望 401

# 带 token
curl -X POST http://staging-kb:5010/multi \
  -H "Authorization: Bearer $KB_SERVICE_TOKEN" \
  -H 'Content-Type: application/json' -d '{"question":"FSHD 是什么"}'
# 期望 200 + chunks

# 语料非空（本次新增的 readiness 门禁）
curl -s http://staging-kb:5010/health/ready
# 期望 200 + status ok。语料为空时是 503 + status "empty_corpus"，
# 不是 "error" —— 这两个的排查方向完全不同
```

> **为什么空语料要挡 readiness**：模型热了、检索链路全通、`/multi` 正常返回 0 条——而 `context-builder.ts` 把「零 chunk 且没有 failureReason」渲染成「（无内容）」，和「这个话题语料里确实没有」完全无法区分。结果就是一个全绿的新环境，对每一个 FSHD 患者说文献里查不到。探测结果非零后永久缓存（稳态零 DB 负载），零 / 未知每 30 秒重探一次，所以 `npm run kb:ingest` 灌完不用重启就会翻绿。探测**不确定**时（比如后端不支持行数查询）不拉黑 readiness。

并发检查（本次新增）：kb-service 换成了 `ThreadingHTTPServer`，`/multi` 由 `KB_MAX_CONCURRENT_SEARCHES`（默认 2，和 `KB_PG_POOL_MAX` 对齐）的信号量限流，排队超过 `KB_SEARCH_QUEUE_WAIT_SECONDS`（默认 20，故意低于调用方 30s 超时）返回 503 `kb_busy`。健康探针走的是廉价路径，**不会**再排在一次搜索后面。

### 2.3 核心功能端到端

按从浅到深跑：

#### A. 注册 / 登录 / OTP

- [ ] staging 手机号注册 → OTP 发送成功
- [ ] 同号几乎同时发两次 register → 第一个 201，第二个 **409**（不是 500）
- [ ] 故意改错腾讯 SMS key，发 OTP → 500；**改回正确 key 仍能成功**（前一次失败没占日限额）
- [ ] 5 次错密码连续登录 → 第 6 次 429 + retryAfter

#### B. 患者档案 + 文档上传

- [ ] 上传 MRI 报告（OCR `classifiedType: 'muscle_mri'`）→ 201 + DB 写入 `document_type = 'mri'`
- [ ] 上传 `Content-Type: text/html` 的文件 → 拉回时响应头是 `application/octet-stream` + `Content-Disposition: attachment` + `X-Content-Type-Options: nosniff`
- [ ] 用户 A 创建 submission，用户 B 塞 A 的 submissionId 调 `POST /me/measurements` → **404**
- [ ] 没有 profile 的新账号上传 → **404**（在 storage.save 之前）
- [ ] 路径穿越尝试 `?storageUri=local://../etc/passwd` → 被拒
- [ ] **本次新增**：上传一份接近 10 MB 上限的扫描件 → 应该能传完。超时预算现在按体积算（45s 固定 + 每 40 字节 1ms，上限 240s），此前是死的 60s，接近上限的文件无论重试几次都传不完
- [ ] **本次新增**：上传一份超过 `OCR_MAX_PDF_PAGES`（默认 30 页）的 PDF → 期望**明确报错**并提示拆分，而不是静默截断。截断意味着第 35 页的 FSHD1-POSITIVE 结论凭空消失且下游无从分辨

#### C. AI 问答（核心）

- [ ] 同意状态 `none` → **403** + `code: consent_required`
- [ ] 切 `basic`：问「FSHD 是什么」→ token 逐字流入，末尾 `[N]` 引用可点
- [ ] 切 `precise`：问「我的 D4Z4 重复数怎么解读」→ 能引用本人数值，audit 行 `usedPersonalData=true`
- [ ] 问到一半切后台 → 后端日志出现 `res.on('close')` + SiliconFlow 调用 abort
- [ ] **本次新增**：把 kb-service 停掉，再问一次 → 期望回答降级（「检索失败」提示）但 `/api/healthz/ready` 仍是 **200**，站点照常。这是本次 readiness 改动要保住的契约
- [ ] **本次新增**：用两个不同账号从同一出口 IP 连打 `/ai/ask` → 限流按 user id 分桶，A 打满不影响 B（此前按 IP 分桶，整个运营商 NAT 后面的患者共享 6 次/分钟）

#### D. 同意 / 共享偏好 / 删除权

- [ ] 同一账号开 2 个 tab 几乎同时点 `dataDonation` → 两个写入互不丢失
- [ ] 切 `precise` → 同意历史页出现新行；切回 `basic` → 又一行
- [ ] AI 审计页 `success` / `error` / `consent_denied` 三类都有
- [ ] **本次新增**：申请注销 → 冷静期内可取消；冷静期过后 sweep 执行，`audit_logs` 里该用户的行仍在（合规证据）但 `phoneNumber` / `email` / `identifier` / `ip` / `userAgent` 已被剥离、带上 `subjectPurgedAt`

#### E. 客户端与页面

- [ ] **本次新增**：在浏览器 devtools 里确认响应头有 `X-Frame-Options: DENY` 和 `frame-ancestors 'none'`；确认页面导航时**不再**发出 `chux-path-change` 的 `postMessage`（此前它把路由和 `documentId` 广播给任意父框架，targetOrigin 是 `*`）
- [ ] **本次新增**：人为让某个页面 throw（改一行临时代码）→ 看到中文兜底页「页面出了点问题 / 你已经保存和上传的内容都在」+ 重新加载按钮，而不是白屏

#### F. KB ingest（运维侧）

- [ ] 500MB 大 PDF 跑 `npm run kb:ingest:dry` → worker RSS 不超过 ~200MB
- [ ] 中文文件名（`档案.md`）在 macOS 创建 → push → Linux 上 `kb:ingest --prune` → **不**被当 orphan 删除
- [ ] 含 `IGNORE ALL PREVIOUS INSTRUCTIONS` 的 chunk 跑 ingest → action log 出现 `injection markers detected` 警告（不阻断）

### 2.4 监控基线采样

部署前记录 staging 当前指标，prod 部署后回头对比：API `5xx` 率、`auth_login_guards` 新增速率、`ai_prompt_audit` status 分布、SiliconFlow 平均调用时长 + 每日 token 消耗。

---

## 3. Prod 部署步骤

按部就班；任一步未通过不进下一步。

### 3.1 维护窗口决策

- 013 / 014 / 016 / 018 是加列、建表、建索引，当前数据规模下毫秒级
- 015 / 017 的 `ADD CONSTRAINT` 取 ACCESS EXCLUSIVE 并做校验扫描；单 api 实例 + `up -d --build` 先停后起，没有并发写者
- 015 会改写 `patient_function_tests.unit` 的历史值（原文保留在 `unit_legacy`）
- **如果决定跑 011/012 的 `VALIDATE CONSTRAINT`**：全表扫描，放维护窗口
- **KB 模型权重**：warm-cache rolling restart ~30s。**首次 deploy 或 volume 重建**：拉 ~2GB 权重，3-5 min 起步。healthcheck budget 是 30 × 15s = 7.5 min。**本次起，超时不再拖垮整站**——api 只等 kb 的 `service_started`，KB 慢只降级 AI 问答。仍然建议 pre-warm：

  ```bash
  docker compose pull kb-service
  docker compose run --rm --no-deps kb-service \
    python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('BAAI/bge-m3')"
  ```

- **宿主机内存**：compose 现在给每个服务设了 `mem_limit`（kb-service 4g + `cpus: '2.0'`、api 3g、postgres 1g、minio 512m、web 128m、caddy 128m），上限合计 8.75 GiB。**最低宿主 8 GB**，这些是「超了杀这个容器」的阈值而不是预留量。设上限的意义是：KB warmup 撑爆内存时内核杀的是 kb-service 自己，而不是按 RSS 打分挑中 postgres。

### 3.2 顺序

1. **确认 `.env`**：`NODE_ENV=production`、`POSTGRES_PASSWORD`、§1.1 整张表。可以先干跑一次：`docker compose --profile prod config -q`（只做插值和校验，不启动任何东西）
2. **PG 启用 vector extension**（如未启用）
3. **备份 prod DB**：`BACKUP_DIR=/mnt/offsite/openrd npm run db:backup`（脚本自带回读校验，见 §1.7）
4. **一条命令拉起整栈**：

   ```bash
   docker compose --profile prod up -d --build
   ```

   迁移随 api 容器 CMD 自动执行（`node dist/db/migrate.js && node dist/index.js`），并且在一个 advisory lock 里跑，所以手工 `npm run db:migrate` 和容器启动撞上不会互相踩。想 migrate-first 而不依赖 auto-run，就在这一步之前手工跑一次 `npm run db:migrate`。

   > kb-service 仍然建议先起（`docker compose --profile prod up -d kb-service` 再 up 全栈），但**不再是硬依赖**：api 现在 `service_started` 就走，`web` 也只等 api `service_started`。

5. **可选 `VALIDATE CONSTRAINT`**（维护窗口 / 低峰，且已跑过 back-fill）
6. **移动端：不需要单独的发版步骤**

   上一步的 `--build` 已经重新导出并发布了 Expo web bundle（`apps/mobile/Dockerfile.web` 跑 `npx expo export --platform web`，产物由 `web` 容器的 nginx 提供，Caddy 反代到 `/*`）。**患者拿到新版本的方式就是刷新页面**，没有商店审核、没有灰度、没有 OTA 通道。

   > v2.4.0 手册这一步写的是「移动端 build 推 EAS / TestFlight / Play Store」。**仓库跑不出原生包**：没有 `apps/mobile/android` / `ios` 目录，没有 `extra.eas.projectId`，`eas-cli` 和 `expo-updates` 都不在依赖里。本次新增的 `apps/mobile/eas.json` 是把版本号策略写进仓库（`appVersionSource: local`，production profile 开 `autoIncrement`），并**故意留空 `submit`**——签名密钥和商店账号还没定，凭空写一个 submit 块会读起来像已经验证过。真要做原生分发，那是一次独立的工作：`prebuild`、bundle id（`app.json` 已填 `com.fshdyouth.jiyutong`）、签名、备案、商店合规材料，都不在本次发布范围。
   >
   > 需要给团队装 APK 试用时，用 `eas build --profile preview`（会在 EAS 上跑，需要先 `eas login` 并创建 project；这条路径**没有**在本次发布中验证过）。

7. **盯监控 1 小时**（见 §3.3）
8. **24 小时复盘**（见 §3.4）

### 3.3 上线后第一小时盯什么

先确认健康端点：

```bash
# 宿主机上（loopback，拿得到完整 payload）
docker compose --profile prod exec -T api \
  node -e "fetch('http://127.0.0.1:4000/api/healthz').then(r=>r.json()).then(j=>console.log(JSON.stringify(j,null,2)))"
```

> **注意**：从公网 `curl https://ai.fshdyouth.com/api/healthz` 现在只会拿到 `{status, ready, requestId, components:{<name>:{status}}}`。原始 pg 错误串、`http://kb-service:5010`、解析器在容器里的绝对路径、Python 版本、KB 的 `lastError` 一律不再进响应体——它们进日志，和回给调用方的 `requestId` 同一行。所以排查时**要么在宿主上走 loopback，要么按 requestId grep 日志**，不要指望公网响应告诉你原因。健康时不打日志（compose 探针 15 秒一次）。

各组件语义（本次调整）：

- `ready: false` 只可能来自 **database 或 embedded OCR 挂了**，或者正在 draining。这两个是「没有它任何请求都服务不了」的组件。
- `status: degraded` + `ready: true`：KB 挂了 / KB 正在 warming / `empty_corpus` / MinIO 连不上 / AI key 没配。**站点仍在服务**，但有东西坏了，要处理。
- `storage` 组件只在 `STORAGE_PROVIDER=minio` 时探测，探 `/minio/health/live`。**任何 HTTP 应答（含 403/404）都算可达**，只有连接失败 / DNS 失败 / 超时才算挂——托管 S3 兼容 endpoint 不会被误报为宕机。

然后盯这几项：

- API `5xx` 率：基线 +50% 以上立刻准备回滚
- `/api/profiles/me/documents/upload` 失败率
- `auth.register_failed` audit 行（`reason=exists_concurrent`）突增 = 有人在猜手机号
- SiliconFlow 调用量 + token 消耗：比 dev 估算高 2× 就关注
- KB service 容器 RSS：`mem_limit` 是 4g，逼近就是要么调大要么查泄漏（此前这条只能靠人盯，现在超了会被 cgroup 杀掉并由 `restart: unless-stopped` 拉起，日志里会有 OOMKilled）
- **本次新增**：`docker compose --profile prod logs api | grep -i 'kb_busy\|shed'` —— KB 限流开始丢请求说明并发超过了 `KB_MAX_CONCURRENT_SEARCHES`

### 3.4 24 小时复盘 grep

```sql
-- audit 错误聚集？
SELECT status, error_detail, count(*)
FROM ai_prompt_audit
WHERE created_at > now() - interval '24 hours' AND status != 'success'
GROUP BY status, error_detail
ORDER BY count(*) DESC LIMIT 20;

-- 文档上传失败模式
SELECT document_type, status, count(*)
FROM patient_documents
WHERE uploaded_at > now() - interval '24 hours'
GROUP BY 1, 2;

-- 保留期 sweep 有没有在跑（本次新增；6 小时一轮）
SELECT min(occurred_at) FROM audit_logs;          -- 不应早于 180 天前
SELECT min(created_at)  FROM ai_prompt_audit;     -- 同上
SELECT count(*) FROM otp_verification_codes
 WHERE expires_at < now() - interval '24 hours';  -- 期待 0
```

### 3.5 全新部署：KB 语料 seed（空库才需要）

§3.2 是「升级既有 prod」的视角。**全新环境**跑完 migration 后 `kb_chunks` 是空的。**本次起这个状态会被 readiness 抓住**：kb-service 返回 503 + `status: "empty_corpus"`，api 的 `/healthz` 把它作为独立组件状态透出（不是笼统的 `error`），所以你会在部署当天就知道，而不是等患者反馈「AI 说文献里没有」。

kb-service 镜像里没有 ingest 脚本 / 源文档，没法在 prod 现场重新 ingest。正确做法是从一个已 ingest 好的环境整表搬 `kb_chunks`：

```bash
# 1. 源环境 dump：data-only
pg_dump -U <user> -d fshd_openrd -t kb_chunks --data-only --no-owner --no-privileges \
  | gzip > /tmp/kb_chunks.sql.gz          # 12352 chunks ≈ 66MB gzip

# 2. 传到新机
scp /tmp/kb_chunks.sql.gz <新机>:/tmp/

# 3. 新机导入
gunzip -c /tmp/kb_chunks.sql.gz | grep -v '^SET transaction_timeout' \
  | docker compose --profile prod exec -T postgres psql -U postgres -d fshd_openrd -v ON_ERROR_STOP=1

# 4. 验证
docker compose --profile prod exec -T postgres psql -U postgres -d fshd_openrd \
  -tc "SELECT count(*) FROM kb_chunks;"
```

⚠️ **PG 版本兼容**：`pg_dump` 若是 PG17（macOS Homebrew 默认）而 prod 是 pg16，dump 头部的 `SET transaction_timeout = 0` PG16 不认，`ON_ERROR_STOP` 会中断导入。`grep -v` 滤掉即可。

导入写入 migration 006 建好的 hnsw 索引，随 COPY 增量维护，12352 行约 2-5 分钟，无需 REINDEX。kb-service 实时查 pg，**导入完无需重启**——readiness 的语料探测在缓存未命中时每 30 秒重探一次，会自己翻绿。

> ⚠️ 源语料（`content/medical-kb/source/`，547 MB / 235 个文件）是 gitignore 的，目前只在一台开发机上。`scripts/kb-ingest.py` 和 `scripts/kb_parsers/` 都在仓库里，能重新 ingest——**前提是那份源语料还在**。把它纳入常规备份是一项独立的待办。

---

## 4. 回滚

### 4.1 Migration 回滚

**用 runner，不要手工 `psql < *_down.sql`。**

```bash
# 按倒序回滚。id 可以写 018、文件名 stem、forward 文件名或 _down 文件名
npm run db:migrate:down -- 018
npm run db:migrate:down -- 017
npm run db:migrate:down -- 016
npm run db:migrate:down -- 015
npm run db:migrate:down -- 014
npm run db:migrate:down -- 013
```

`--down` 在**同一个事务**里做两件事：跑 `_down.sql`，并 `DELETE FROM schema_migrations WHERE id = <forward 文件名>`。

> **为什么必须这样**：v2.4.0 手册 §4.1 让你 `psql $PROD_DB < db/migrations/012_..._down.sql`。那条命令不碰 ledger，于是回滚后 ledger 仍宣称 012 已应用，而 schema 里什么都没有。后续 roll-forward 时 `applied.has(file)` 直接跳过，**什么都不打印、退出码 0**，约束永久缺失而部署报告成功。这正是 PR #58 修掉的那一类静默失败，只不过是通过仓库自己文档里的回滚流程复现的。
>
> 另外那些 `_down.sql` 在本分支上已经不再自带 `BEGIN` / `COMMIT`（runner 会包），所以直接 `psql <` 跑是**非事务**的；`--down` 把事务还了回来。
>
> ledger 里没有对应行时 `--down` 会拒绝执行并提示 `--force`——那是给「已经按旧流程手工跑过 down 脚本、现在只需要清掉 ledger 行」的人用的。

**回滚前必须知道的两件事**：

- **016（软删除）**：`_down` 会 `DROP COLUMN deleted_at`。**所有被患者撤回的随访记录会立刻复活**，重新进入趋势线、首页叙述和 AI 检索。这个信息在剩下的数据里无法重建——`deleted_at` 一没，「这条被撤回过」这件事就不存在了。如果生产上已经有人用过撤回功能，**回滚 016 之前先把它捞出来存档**：

  ```sql
  SELECT 'function_tests' AS t, id, deleted_at FROM patient_function_tests WHERE deleted_at IS NOT NULL
  UNION ALL SELECT 'symptom_scores', id, deleted_at FROM patient_symptom_scores WHERE deleted_at IS NOT NULL
  UNION ALL SELECT 'followup_events', id, deleted_at FROM patient_followup_events WHERE deleted_at IS NOT NULL;
  ```

- **015（unit 归一化）**：`_down` 先 DROP CHECK（顺序不能反——CHECK 还在时往回写自由文本会 23514），再从 `unit_legacy` 还原 `unit`。它**不会**顺手删掉 `unit_legacy`：那一列就是恢复记录，而回滚恰恰是最可能需要用它两次的时刻。如果数据库是被**旧版 015**（没有 `unit_legacy` 那版）迁过来的，`_down` 会 RAISE 一条 WARNING 点名哪些数据无法恢复，并指向 §1.7 的迁移前备份。

其余四条（013 / 014 / 017 / 018）的 `_down` 都是 `DROP COLUMN` / `DROP TABLE` / `DROP INDEX`，无隐藏数据损失——但 014 掉的是 `account_deletion_requests`，那是删除权的合规账本，回滚前先 dump 一份。

### 4.2 版本回滚

本仓库是 compose + Caddy，**不是 k8s**（v2.4.0 手册 §4.2 给的是 kubectl 示例，只是示意）。实际的回滚是按 git tag 重建：

```bash
git fetch --tags
git checkout v2.4.0
docker compose --profile prod up -d --build
```

三个自建服务（api / kb-service / web）没有 `image:` 键，compose 用 `<project>-<service>:latest` 标签，所以回滚靠重建而不是换 tag——比换 tag 慢，但是可执行且版本明确。`minio` / `postgres` / `caddy` 三个外部镜像都已固定版本（`minio/minio:RELEASE.2025-09-07T16-13-09Z`、`pgvector/pgvector:pg16`、`caddy:2-alpine`），重建不会漂移。

`KB_SERVICE_TOKEN` 留在 `.env` 里对旧版本无害。**回滚后不要手工移除**，否则 roll-forward 时新镜像会因缺 token 拒绝启动。

> ⚠️ **回滚到 v2.4.0 之前先想清楚 env**：v2.5.0 新增的三个确认位（`AI_CROSS_BORDER_ACKNOWLEDGED` / `STORAGE_ALLOW_LOCAL` / `MINIO_ALLOW_INSECURE`）和 `POSTGRES_PASSWORD` 在 v2.4.0 的 compose 里不存在，留着无害。反过来，v2.4.0 的 compose 会把 `DATABASE_URL` 硬编码回 compose 本地库——**如果你已经迁到托管 PG，回滚会把 api 指回本地空库**。这条是 v2.5.0 修掉的缺陷，回滚就会把它带回来。

### 4.3 数据回滚

如果迁移期间已有新写入，回滚老版本后新 status 值会变成「未知」。两个选项：接受不一致（老代码读到未知 status 默认按 `uploaded` 处理），或批量映射回去：

```sql
UPDATE patient_documents SET status = 'processed' WHERE status = 'parsed';
UPDATE patient_documents SET status = 'failed'    WHERE status = 'parse_failed';
UPDATE patient_documents SET status = 'uploaded'  WHERE status = 'needs_review';
```

#### 上传文档的备份策略

患者文档（PDF / 影像 / 基因报告）落地路径取决于 `STORAGE_PROVIDER`：

- **`local`**（schema 默认）：写在 named volume `api-uploads`。`docker compose down -v` 会清空它，等于永久丢失全部患者文档。**本次起，生产用 `local` 必须显式设 `STORAGE_ALLOW_LOCAL=true` 才能启动**——此前这是唯一一个完全不经过校验的存储配置，也是「谁都没动过设置」时的默认值。
- **`minio`**（推荐 prod / staging）：写在 bucket `MINIO_BUCKET_NAME`（默认 `medical-reports`），用对象存储自带的版本 / lifecycle / 备份机制。`--profile prod` 现在会一并启动 minio 服务。

如果迫不得已在 prod 用 `local`：

- 每天备份 volume：`docker run --rm -v openrd_api-uploads:/uploads -v /backup:/backup alpine tar czf /backup/uploads-$(date +%F).tar.gz -C /uploads .`
- 备份传到独立机器或对象存储
- `docker compose down -v` **绝对禁用**，只用 `docker compose down`

### 4.4 前端回滚

web export 是唯一的分发通道，所以「回滚移动端」就是 §4.2 的整栈回滚——`git checkout v2.4.0 && docker compose --profile prod up -d --build` 会重新导出旧版 bundle，患者刷新页面即回退。没有商店审核延迟，也没有「已安装用户还停在新版本」的问题。

这同时也是**不做原生分发的一个实际好处**，在真的启动商店分发之前值得记住：原生版本发出去就撤不回来，只能靠 hotfix。

---

## 5. 已知关注点

- **`.env.example` 缺 `POSTGRES_PASSWORD`**：`cp .env.example .env` 之后仍需手工加一行，否则 `docker compose config` 就失败。见 §1.0。
- **没有 CI**：仓库里没有 `.github/workflows`，master 分支保护也没有 required status checks。48 个 API 测试文件、24 个移动端测试文件、13 个 Python 测试文件全靠人手工跑。发布前请手工执行 `npm run lint && npm test` 和 `python -m pytest apps/report-manager/tests scripts/kb_parsers`。
- **隐私政策仍是占位文本**：`apps/mobile/lib/legal-content.ts` 是五条一句话的 bullet，不满足 PIPL 第 17 条的告知要求，也没有任何表记录用户接受过哪个版本的协议；第三方 LLM 的单独同意开关存在且默认关闭，但没有点名接收方。本次发布**没有**修这一项，它是独立的产品/法务工作。参见 `docs/release-checklist.md` §4 的对应门禁。
- **没有未成年人 / 监护人同意流程**：出生日期已在注册时采集，但没有任何逻辑基于它分流。FSHD 有青少年和婴儿型，这是「什么时候发生」而不是「会不会发生」的问题。
- **KB 源语料单点**：见 §3.5 末尾。
- **`docs/cloud-tencent-docker.md`** 已标注为不适用于生产：它给的 compose 命令不带 `--profile prod`（没有 Caddy、没有 HTTPS、没有 MinIO），且仍在描述已退役的 Chroma 配置。生产以本手册为准。
- **NFC orphan 转换**：首次 `kb:ingest` 时 macOS NFD 形态的中文文件名会被 NFC 归一化，看起来像「全部重 ingest 一次」，是预期行为。

## 6. 联系人

- 后端 (Node API / orchestrator)：……
- KB service (Python)：……
- DBA：……
- 前端 (Expo web export)：……

部署中遇到本手册没覆盖的状况，先看 `docs/release-checklist.md` 通用规约，再升级到团队。
